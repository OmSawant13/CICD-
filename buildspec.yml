version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - node -v
      - npm -v
      - npm install --include=dev

  build:
    commands:
      - npm run build

artifacts:
  files:
    - "**/*"
  base-directory: dist